in powershell 
cd "C:\Program Files\MySQL\MySQL Server 8.0\bin"
.\mysql -u root -p

apple123

USE edgeurl;

SELECT * FROM urls;

1. Better short-code generation

Use Base62 generator.

2. Collision handling

Because random generation can still duplicate.

3. Rate limiting

This protects abuse, but core shortening must work first.